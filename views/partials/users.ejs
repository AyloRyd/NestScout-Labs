<table class="stripes">
    <thead>
    <tr>
        <th></th>
        <th>ID</th>
        <th>Name</th>
        <th>Email</th>
        <th>Created at</th>
        <th>To user's listings</th>
        <th>To user's bookings</th>
    </tr>
    </thead>
    <tbody>
    <% users.forEach(user => { %>
        <tr id="users-<%= user.id %>">
            <td>
                <label class="checkbox">
                    <input type="checkbox" name="selectedUsers" value="<%= user.id %>">
                    <span></span>
                </label>
            </td>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= new Date(user.created_at).toLocaleDateString() %></td>
            <td>
                <button class="filled-button">
                    Listings
                </button>
            </td>
            <td>
                <button class="filled-button">
                    Bookings
                </button>
            </td>
        </tr>
    <% }) %>
    </tbody>
</table>